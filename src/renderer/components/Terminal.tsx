import React from 'react';

export const Terminal: React.FC = () => {
  return (
    <div className="terminal">
      {/* Terminal implementation will go here */}
    </div>
  );
};

export default Terminal; 